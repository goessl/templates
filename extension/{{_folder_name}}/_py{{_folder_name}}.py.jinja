"""Pure Python implementation of the {{_folder_name}} module."""

def foo(a, b):
    """Add two numbers.
    
    Returns the sum of `a` and `b`.
    
    $$
        a + b
    $$
    
    Python implementation.
    """
    return a + b

class A:
    """Sum object."""
    
    def __init__(self, a, b):
        self.a, self.b = a, b
